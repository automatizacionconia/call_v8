export const environment = {
  production: false,
  apiUrl: `${document.getElementById("baseURL").value}/api`,
  baseUrl: document.getElementById("baseURL").value,
  appName: APP.APP_NAME,
  version: APP.APP_VERSION,
};
